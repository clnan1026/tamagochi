{
  "name": "tamagotchi-desktop",
  "productName": "Tamagotchi",
  "version": "0.1.0",
  "description": "A pixel-art pet that roams your desktop.",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test src/app/*.test.js",
    "package": "electron-packager . Tamagotchi --platform=darwin --arch=arm64 --icon=assets/icon.icns --overwrite"
  },
  "devDependencies": {
    "electron": "latest",
    "electron-packager": "^17.1.2"
  }
}
